# 2026-07-16

## 博客从 Vercel 迁移到 Cloudflare Pages

- 创建 `blog.source/source/_headers` 替代 `vercel.json`，配置安全响应头
- 删除 `vercel.json`（项目根目录）
- 更新 `blog.source/_config.yml` 的 `skip_render`，移除 `vercel.json` 条目
- 更新 `AGENTS.md` 所有 Vercel 引用改为 Cloudflare Pages，新增 `_headers` 说明
- Cloudflare Pages 项目地址：`lcy362-github-io.pages.dev`
- DNS 已在 Cloudflare 管理，自定义域 `lichuanyang.top` 待用户在面板添加
- 部署流程不变：`deploy.sh -d` → 推送到 `lcy362/lcy362.github.io.git` → Cloudflare 自动同步

## 添加 Adsterra 广告

- 中文站 + 英文站 `_config.butterfly.yml` 的 `inject.bottom` 添加了 Social Bar 占位
- Smartlink 改为点击触发模式：用户首次点击/触摸页面时自动打开新窗口，localStorage 记录时间戳，5 分钟冷却（与 video.lichuanyang.top 一致）
- 页脚 Smartlink 已还原，不再静态展示
- 用户需要从 Adsterra Dashboard 获取实际代码后替换 `YOUR_ADSTERRA_SMARTLINK_URL` 和 `<!-- TODO: ... -->` 占位符
